# Optional. Without these, generation uses the heuristic storyboard
# and TTS uses the local Edge voices.

LLM_API_KEY=
LLM_BASE_URL=https://api.openai.com/v1
LLM_MODEL=gpt-4o-mini

AZURE_SPEECH_KEY=
AZURE_SPEECH_REGION=
